# V5 代码生成分批执行计划

> 总计 36 个文件操作，分 5 批执行，预估消耗 ~93 次请求（剩余 154 次，余量充足）。
>
> **每批执行完毕后必须构建 + 浏览器验证，确认不破坏已有功能。**

---

## Batch 1：Zustand 重构 + 基础设施（P0）

> **目标**：完成状态管理重构，奠定 V5 基础。现有功能不受影响。
> **预估请求数**：~20 次 | **风险等级**：中（流式 SSE 逻辑迁移）

### 文件清单

| # | 文件路径 | 操作 | 行数 | 说明 |
|---|---------|------|------|------|
| 1 | `app/lib/storage.ts` | 🟢 新增 | 80 | localStorage 封装 + key 管理 + 容量检测 |
| 2 | `app/types/chatTypes.ts` | 🟡 追加 | +50 | V5 新增类型：pinned/archived/CodeAttachment/PromptTemplate/FavoriteItem/ThemeMode |
| 3 | `app/store/useChatStore.ts` | 🔴 重构 | 250 | 自定义 Hook → zustand create + persist，4 步迁移 |
| 4 | `app/stores/useThemeStore.ts` | 🟢 新增 | 60 | 主题 zustand store（mode/theme/applyTheme） |
| 5 | `app/stores/useTemplateStore.ts` | 🟢 新增 | 120 | 模板 CRUD + 搜索 + 排序 + persist |
| 6 | `app/stores/useFavoriteStore.ts` | 🟢 新增 | 80 | 收藏管理 + persist |
| 7 | `app/chat/page.tsx` | 🟡 小改 | ~10 | import 切换到 zustand store |
| 8 | `app/package.json` | 🟡 小改 | +3 | 新增 @dnd-kit/jszip/file-saver 依赖声明 |

### 验证清单

- [ ] `node next build` 构建通过
- [ ] 浏览器：新建会话 → 发送消息 → 流式输出正常
- [ ] 浏览器：停止生成 → 错误卡片展示 → 重试
- [ ] 浏览器：切换会话/学习模式/删除会话
- [ ] 浏览器：刷新页面，历史会话仍在（persist 生效）
- [ ] 浏览器控制台无 React 警告

### 回滚方案

重构后的 useChatStore.ts 如果出问题，直接恢复 .backup 版本即可。

---

## Batch 2：暗黑模式 + AI 标题（P0）

> **目标**：实现亮/暗主题切换 + 会话标题自动生成。
> **预估请求数**：~15 次 | **风险等级**：低

### 文件清单

| # | 文件路径 | 操作 | 行数 | 说明 |
|---|---------|------|------|------|
| 1 | `app/styles/theme.css` | 🟢 新增 | 80 | 暗色 CSS Variables 定义（[data-theme="dark"]） |
| 2 | `app/components/theme/ThemeToggle.tsx` | 🟢 新增 | 40 | 主题切换按钮（亮/暗/跟随系统三态） |
| 3 | `app/layout.tsx` | 🟡 修改 | ~8 | html 添加 data-theme 属性、引入 theme.css |
| 4 | `app/styles/chatStyles.css` | 🟡 批量替换 | ~150 | 硬编码颜色 → CSS Variables（background/color/border/shadow） |
| 5 | `app/api/generate-title/route.ts` | 🟢 新增 | 50 | AI 标题生成 POST 接口 |

### 验证清单

- [x] `node next build` 构建通过
- [x] 浏览器：点击主题按钮 → 亮/暗切换正常
- [x] 浏览器：切换系统主题 → 自动跟随（如已配置）
- [x] 浏览器：暗色模式下所有组件可读（文字/气泡/按钮/边框）
- [x] 浏览器：新建会话发消息 → 标题自动生成（“闭包：像小盒子一样记东西”）
- [x] localStorage 中 theme_preference 和 chat_sessions 数据正确

---

## Batch 3：会话管理增强 + 全局搜索（P0+P1）

> **目标**：会话搜索/重命名/置顶/归档/拖拽/批量删除 + 消息级搜索。
> **预估请求数**：~20 次 | **风险等级**：低（全部新增文件）

### 文件清单

| # | 文件路径 | 操作 | 行数 | 说明 |
|---|---------|------|------|------|
| 1 | `app/hooks/useSessionManager.ts` | 🟢 新增 | 200 | 重命名/置顶/归档/拖拽排序/批量选择/删除 |
| 2 | `app/hooks/useSearch.ts` | 🟢 新增 | 80 | 消息搜索 + 高亮匹配 |
| 3 | `app/lib/searchUtils.ts` | 🟢 新增 | 60 | 搜索匹配算法 + 高亮片段提取 |
| 4 | `app/components/session/SessionSearchBar.tsx` | 🟢 新增 | 40 | 会话搜索输入框 |
| 5 | `app/components/session/SessionItem.tsx` | 🟢 新增 | 120 | 单个会话项（右键菜单/双击重命名/Ctrl+Click多选） |
| 6 | `app/components/session/SessionGroup.tsx` | 🟢 新增 | 60 | 分组容器（置顶/全部/已归档） |
| 7 | `app/components/session/SessionBatchBar.tsx` | 🟢 新增 | 50 | 批量操作栏（全选/删除/退出） |
| 8 | `app/components/search/GlobalSearch.tsx` | 🟢 新增 | 120 | 全局消息搜索弹层 + 高亮定位 |

### 验证清单

- [ ] `node next build` 构建通过
- [ ] 浏览器：搜索框输入 → 会话列表实时过滤
- [ ] 浏览器：右键会话 → 菜单弹出（重命名/置顶/归档/删除）
- [ ] 浏览器：双击会话标题 → 内联编辑
- [ ] 浏览器：拖拽会话 → 顺序变化并持久化
- [ ] 浏览器：Ctrl+Click → 多选模式 → 批量删除
- [ ] 浏览器：全局搜索 → 命中消息高亮 → 点击跳转

---

## Batch 4：模板 + 收藏 + 导出（P1）

> **目标**：Prompt 模板市场 + 消息收藏 + JSON/Markdown 导出。
> **预估请求数**：~17 次 | **风险等级**：低

### 文件清单

| # | 文件路径 | 操作 | 行数 | 说明 |
|---|---------|------|------|------|
| 1 | `app/components/template/TemplatePanel.tsx` | 🟢 新增 | 100 | 模板面板抽屉（内置+自定义+搜索+分类） |
| 2 | `app/components/template/TemplateCard.tsx` | 🟢 新增 | 50 | 模板卡片（标题/描述/使用按钮） |
| 3 | `app/components/template/TemplateForm.tsx` | 🟢 新增 | 120 | 创建/编辑模板表单（名称/内容/分类） |
| 4 | `app/components/favorites/FavoritesDrawer.tsx` | 🟢 新增 | 120 | 收藏夹抽屉（消息列表/搜索/复制/取消收藏） |
| 5 | `app/components/export/ExportMenu.tsx` | 🟢 新增 | 80 | 导出菜单（JSON/Markdown 选项） |
| 6 | `app/hooks/useExport.ts` | 🟢 新增 | 60 | 导出状态管理 + 触发逻辑 |
| 7 | `app/lib/exportUtils.ts` | 🟢 新增 | 100 | formatJSONExport / formatMarkdownExport 函数 |

### 验证清单

- [ ] `node next build` 构建通过
- [ ] 浏览器：打开模板面板 → 内置模板可见 → 搜索过滤
- [ ] 浏览器：创建自定义模板 → 保存 → 在列表中可见
- [ ] 浏览器：编辑/删除自定义模板
- [ ] 浏览器：点击模板 → 输入框自动填入 prompt
- [ ] 浏览器：收藏消息 → 收藏夹抽屉中可见
- [ ] 浏览器：取消收藏 → 从列表移除
- [ ] 浏览器：导出 JSON → 下载文件，内容完整
- [ ] 浏览器：导出 Markdown → 下载 .md 文件，格式正确

---

## Batch 5：现有组件改造 + 代码交互 + 学习计划（P1+P2）

> **目标**：消息气泡增强（收藏/解释代码按钮）+ 代码上传/插入 + 学习计划 + 样式收尾。
> **预估请求数**：~21 次 | **风险等级**：低~中（修改现有组件）

### 文件清单

| # | 文件路径 | 操作 | 行数 | 说明 |
|---|---------|------|------|------|
| 1 | `app/components/ChatMessageBubble.tsx` | 🟡 修改 | +30 | MessageActions 加 ⭐ 收藏按钮；CodeBlock 加「解释代码」按钮 |
| 2 | `app/components/ChatWindow.tsx` | 🟡 修改 | +40 | 输入区工具栏（📎 附件 + `</>` 代码） |
| 3 | `app/components/SessionList.tsx` | 🟡 修改 | +60 | 集成 SessionSearchBar + SessionGroup + BatchBar |
| 4 | `app/components/code/CodeUpload.tsx` | 🟢 新增 | 80 | 代码粘贴/文件上传入口 |
| 5 | `app/components/code/CodeInsertModal.tsx` | 🟢 新增 | 80 | 手动插入代码弹层 |
| 6 | `app/components/plan/PlanForm.tsx` | 🟢 新增 | 100 | 学习计划输入表单（科目/时长/难度/目标） |
| 7 | `app/components/plan/PlanCard.tsx` | 🟢 新增 | 80 | 学习计划展示卡片（摘要/大纲） |
| 8 | `app/styles/chatStyles.css` | 🟡 追加 | +120 | 新增组件样式（template/favorite/export/code/plan） |

### 验证清单

- [ ] `node next build` 构建通过
- [ ] 浏览器：机器人消息 hover → 出现 ⭐ 按钮 → 点击收藏
- [ ] 浏览器：代码块 → 出现「解释代码」→ 点击触发解释
- [ ] 浏览器：输入区附件按钮 → 选择文件 → 预览附件
- [ ] 浏览器：代码插入 → 粘贴代码 → 确认插入
- [ ] 浏览器：学习计划表单 → 填写 → 提交 → 展示卡片
- [ ] 浏览器：暗色模式下所有新组件样式正常
- [ ] 浏览器：全量回归测试（所有现有功能正常）

---

## 请求数汇总

| 批次 | 文件操作 | 构建+验证 | 修复缓冲 | 预估总计 | 累计 |
|------|---------|----------|---------|---------|------|
| Batch 1 | 8 | 6 | 6 | **20** | 20 |
| Batch 2 | 5 | 4 | 6 | **15** | 35 |
| Batch 3 | 8 | 5 | 7 | **20** | 55 |
| Batch 4 | 7 | 5 | 5 | **17** | 72 |
| Batch 5 | 8 | 5 | 8 | **21** | 93 |
| **总计** | **36** | **25** | **32** | **93** | **剩余 61 次** |

> 154 次可用 - 93 次预估 = **61 次余量**，足够应对编译修复和回归验证。

---

## 执行顺序约束

```
Batch 1 → 必须先完成（zustand 重构是所有后续批的前提）
Batch 2 → 可在 Batch 1 后立即执行（暗黑模式独立性强）
Batch 3 → 依赖 Batch 1（useSessionManager 依赖 zustand store）
Batch 4 → 依赖 Batch 1（useExport 依赖 chatStore 的 sessions）
Batch 5 → 依赖 Batch 2-4（修改现有组件，集成前面所有新功能）
```

---

## 前置准备

执行 Batch 1 前需要先安装新依赖：

```powershell
cd c:\Users\TunTun\Desktop\demo\aichat
npm install @dnd-kit/core @dnd-kit/sortable jszip file-saver @types/file-saver
```

---

## 关键提醒

1. **每批完成后必须验证**：构建通过 + 浏览器功能正常 + 控制台无报错
2. **Batch 1 最关键**：zustand 重构后立即全量验证现有功能
3. **回滚预案**：所有修改通过 git 管理，出问题可 `git checkout` 回退
4. **localStorage 兼容**：zustand persist key `chat_sessions` 与现有一致，旧数据自动读取
